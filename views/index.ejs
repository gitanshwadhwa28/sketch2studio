<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sketch2Studio</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
            overflow: hidden;
        }
        .container {
            text-align: center;
            animation: fadeIn 2s ease-in-out;
        }
        h1 {
            font-size: 3em;
            color: #333;
            margin-bottom: 0.5em;
        }
        p {
            font-size: 1.2em;
            color: #666;
            margin-bottom: 1.5em;
        }
        .btn {
            padding: 0.75em 1.5em;
            font-size: 1em;
            color: #fff;
            background-color: #007BFF;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #0056b3;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to Sketch2Studio</h1>
        <p>Convert your product sketches to professional images and automate marketplace integration.</p>
        <button class="btn" onclick="startApp()">Get Started</button>
        <!-- <h3>Upload Your Sketch</h3>
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <input type="file" name="sketch" accept="image/*" required />
            <button type="submit">Upload and Analyze</button>
        </form> -->
    </div>
    <script>
        function startApp() {
            alert('Starting the application...');
        }
    </script>
</body>
</html>